class Solution:
    def armstrongNumber (self, n):
        # code here 
        val=str(n)
        sum=0
        res=[]
        for i in val:
            res.append(int(i))
        for j in res:
            sum+=j**3
        if sum==n:
            return 'true'
        else:
            return 'false'
