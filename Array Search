#User function Template for python3

class Solution:
    #Complete the below function
    def search(self,arr, N, X):
        for i in range(N):
            if arr[i]==X:
                return i
        else:
            return -1
